## 一、选择题（每题 5 分，共 20 分）
1. 下列关于 Java 中 `int` 和 `Integer` 的说法，错误的是（ ） #flashcard
    A. `int`是基本数据类型，不具备对象特性，无法调用方法
    B. `Integer`是`int`的封装类，底层通过`private int value`存储数值
    C. `Integer obj = 10;` 这行代码本质是自动装箱，等价于`Integer obj = Integer.valueOf(10);`
    D. 可以直接通过 `obj.value` 的方式获取 `Integer` 对象中存储的数值
<!--ID: 1762146670332-->






2. 执行以下 Java 代码，控制台输出结果为（ ）
```java
public class Test {
    public static void main(String[] args) {
        int a = 20;
        Integer b = Integer.valueOf(a);
        Integer c = new Integer(20);
        System.out.println(b.equals(c));
        System.out.println(b == c);
    }
}
```
A. true、true
B. true、false
C. false、true
D. false、false

3. 下列场景中，必须使用`Integer`而非`int`的是（ ）
    
    A. 定义变量存储学生的年龄（如 18 岁）
    
    B. 计算两个整数的和（如 10 + 20）
    
    C. 向`ArrayList`集合中添加整数数据
    
    D. 声明方法的参数，接收用户输入的整数
    
4. 关于 “装箱” 和 “拆箱” 的描述，正确的是（ ）
    
    A. 手动装箱必须使用`new Integer(int value)`构造方法，不能用`Integer.valueOf()`
    
    B. 自动拆箱会自动调用`Integer`类的`intValue()`方法获取底层数值
    
    C. `int`类型变量可以直接与`Integer`类型变量进行`==`比较，比较的是数值大小
    
    D. 装箱时如果`int`数值超出`Integer`的取值范围（-2^31 ~ 2^31-1），会直接报错
    

## 二、填空题（每空 3 分，共 15 分）

1. `Integer`类中的`value`成员变量被`private`修饰，其作用是______，同时该变量还被`final`修饰，意味着`Integer`对象创建后，其存储的数值______。
2. 执行代码`Integer num1 = 127; Integer num2 = 127; System.out.println(num1 == num2);`，输出结果为______，这是因为`Integer`的`valueOf()`方法对______范围内的整数做了缓存优化；若将数值改为 128，输出结果则为______。

## 三、代码分析题（共 35 分）

1. 阅读以下代码，指出其中存在的问题，并说明原因，同时修改代码使其能正确运行（15 分）

java

运行

```java
public class IntegerTest {
    public static void main(String[] args) {
        // 定义Integer对象
        Integer score = new Integer(95);
        // 尝试修改score存储的数值
        score.value = 100;
        // 打印修改后的值
        System.out.println("修改后的分数：" + score.intValue());
    }
}
```

2. 分析以下代码的执行流程，说明每一步涉及的 “装箱” 或 “拆箱” 操作，并写出最终控制台输出结果（20 分）

java

运行

```java
public class BoxUnboxTest {
    public static void main(String[] args) {
        // 步骤1：定义int变量
        int math = 88;
        // 步骤2：将int转换为Integer
        Integer mathObj = math;
        // 步骤3：调用Integer的方法获取数值的字符串形式
        String mathStr = mathObj.toString();
        // 步骤4：将Integer对象转换为int
        int newMath = mathObj + 5;
        // 步骤5：打印结果
        System.out.println("数学成绩字符串：" + mathStr);
        System.out.println("加分后的数学成绩：" + newMath);
    }
}
```