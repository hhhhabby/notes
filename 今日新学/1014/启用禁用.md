很简单的三层架构
然后就是
Xml 里面的
<!--    菜品的启用和停售-->  
    <update id="startOrStop">  
        UPDATE dish    <set>  
        <if test="status !=null">  
            status = #{status},        </if>  
    </set>  
        where id = #{id}    </update>